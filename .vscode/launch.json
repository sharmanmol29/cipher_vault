{
    "configurations": [
        {
            "type": "java",
            "name": "Spring Boot-CipherVaultBackendApplication<ciphervault-backend>",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "mainClass": "com.ciphervault.CipherVaultBackendApplication",
            "projectName": "ciphervault-backend",
            "args": "",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}